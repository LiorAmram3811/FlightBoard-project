using Microsoft.AspNetCore.SignalR;

// Purpose: Enables real-time communication for flight updates between server and connected clients.
namespace FlightBoard.API.Hubs
{
    public class FlightHub : Hub
    {
        // We can add hub methods if needed, but basic broadcast is enough for now
    }
}
